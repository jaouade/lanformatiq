/*CREATE TABLE todos (
    id INT PRIMARY KEY,
    task VARCHAR(250),
    finished NUMBER(1),
    CONSTRAINT id unique
    CONSTRAINT finished CHECK (is_checked IN (1,0))
)
CREATE SCHEMA `todoDatabase` ;
*/